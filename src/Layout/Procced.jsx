import React from 'react';
import Loading from '../Components/Loading/Loading';

const Procced = () => {
    return (
        <div>
            <Loading/>
        </div>
    );
};

export default Procced;